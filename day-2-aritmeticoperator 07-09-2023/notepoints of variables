1) for variables we are passing data manually always like that we are learning (here are are only woeking with the single data)
2)#where we are assign data to the variable in that place only we are assign
3)##based on the how many input required from user we decide the number of variable we need to declare